<?php
    // Php не вміє просто так отримувати дані в форматі json
    // Для цього треба прописати таку команду:
    $_POST = json_decode(file_get_contents('php://input'), true);

    // Ця команда приймає дані, які приходять від клієнта, перетворює їх на рядок і безпосередньо показує їх назад на клієнті.
    // Тобто це і є той наш 'response', який приходить нам від серверу
    echo var_dump($_POST);